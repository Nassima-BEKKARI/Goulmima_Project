{% extends 'layout.html.twig' %}

{% block title %}Reservation
{% endblock %}

{% block content %}
	<div class="hotel_booking_area position">
		<div class="container">
			<div class="row hotel_booking_table">
				<div class="col-md-3">
					<h2>Réservez<br>
						Votre chambre</h2>
				</div>
				<div class="col-md-9">
					{{ form_start(formReservation) }}
					<div class="row">
						<div class="col-md-4">
							<div class="row">
								<div class="form-group">
									{{ form_row(formReservation.dateArrivee) }}
								</div>
								<div class="form-group">
									{{ form_row(formReservation.dateDepart) }}
								</div>
							</div>
						</div>
						<div>
							{{ form_row(formReservation.Chambre) }}
						</div>
						<div>
							{{ form_row(formReservation.reserver) }}
						</div>
					</div>
					{{ form_end(formReservation) }}
				</div>
			</div>
		</div>
	</div>
{% endblock %}
